import BottomNavItem from '../bottomNav/src/bottomNavItem.vue';

/* istanbul ignore next */
BottomNavItem.install = function(Vue) {
  Vue.component(BottomNavItem.name, BottomNavItem);
};

export default BottomNavItem;
